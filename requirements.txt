# Core dependencies for Aircraft Dataset Generator
numpy>=1.21.0
Pillow>=8.3.0
matplotlib>=3.5.0
opencv-python>=4.5.0
scikit-image>=0.18.0
tqdm>=4.62.0

# Optional dependencies for advanced providers
# Install with: pip install aircraft-dataset-generator[tigl]
trimesh>=3.20.0  # For STL mesh loading/processing

# TiGL provider (install separately with conda)
# conda install -c dlr-sc tigl3